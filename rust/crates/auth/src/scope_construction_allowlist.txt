rust/crates/core/src/tenant.rs # Defines TenantScope::new and tests constructor semantics.
rust/crates/api/src/test_support.rs # Central test-only TenantScope construction helper.
rust/crates/api/src/services/project_clone_worker.rs # Background clone worker has no auth-middleware request; builds an org-scoped (nil-user) scope from the server-enqueued attempt's organization_id to resolve a host-matched credential (org-constrained in SQL).
rust/crates/api/src/services/self_fix_pr_worker.rs # Background self-fix PR worker has no auth-middleware request; builds an org-scoped (nil-user) scope from the durable job payload's organization_id before opening the task-scoped PR.
rust/crates/auth/src/middleware.rs # THE auth-middleware constructor: builds the request scope from the validated JWT claims (org/user/axes). This is the single legitimate per-request construction site.
rust/crates/api/src/gateway/ws.rs # WebSocket gateway auth: builds the connection scope from the validated JWT (?token=) before any handler runs, mirroring the HTTP middleware.
rust/crates/api/src/services/agent_reconcile_worker.rs # Background reconcile worker has no auth-middleware request; builds an org-scoped scope from the server-owned agent row to reconcile container state.
rust/crates/api/src/services/cli_image_roll.rs # Background CLI-image roll worker has no auth-middleware request; builds an org-scoped scope from the server-enqueued roll target.
rust/crates/api/src/domain/memory.rs # test-only: with_axes appears only inside #[cfg(test)] for domain policy tests.
rust/crates/api/src/domain/skill.rs # test-only: with_axes appears only inside #[cfg(test)] for domain policy tests.
rust/crates/api/src/domain/context.rs # test-only: with_axes appears only inside #[cfg(test)] for domain policy tests.
rust/crates/api/src/domain/gateway.rs # test-only: with_axes appears only inside #[cfg(test)] for domain policy tests.
rust/crates/api/src/domain/usage_analytics.rs # test-only: with_axes appears only inside #[cfg(test)] for domain policy tests.
rust/crates/api/src/domain/task_context.rs # test-only: with_axes appears only inside #[cfg(test)] for domain policy tests.
rust/crates/api/src/domain/orchestration.rs # test-only: with_axes appears only inside #[cfg(test)] for domain policy tests.
rust/crates/api/tests/context_provenance_test.rs # test-only: integration test builds an axis-scoped TenantScope fixture.

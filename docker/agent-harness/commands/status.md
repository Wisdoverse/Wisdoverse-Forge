Report the current state of the workspace.

Run these checks and present a summary:

1. `git status --short`
2. `git log --oneline -5`
3. `git branch -a`
4. Check if there are uncommitted changes
5. Check if the branch is ahead/behind upstream

Format as a concise status report.
